<Window x:Class="FurnitureCRMClient.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:views="clr-namespace:FurnitureCRMClient.Views"
        Title="Вход в систему" MinHeight="350" Width="400"
        SizeToContent="Height"
        WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <!-- Основные цвета -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#FF1976D2"/>
        <SolidColorBrush x:Key="PrimaryDarkColor" Color="#FF0D47A1"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="#FFFFFFFF"/>

        <!-- Стиль для заголовка -->
        <Style x:Key="TitleTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="22"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#222"/>
        </Style>

        <!-- Стиль для подписи полей -->
        <Style x:Key="LabelTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Foreground" Value="#555"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
        </Style>

        <!-- Стиль для текстовых полей -->
        <Style x:Key="InputTextBoxStyle" TargetType="TextBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для PasswordBox -->
        <Style x:Key="InputPasswordBoxStyle" TargetType="PasswordBox">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="0,0,0,4"/>
            <Setter Property="Padding" Value="8,4"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="#CCC"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="SnapsToDevicePixels" Value="True"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"
                                          Margin="0"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsKeyboardFocused" Value="True">
                    <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль кнопки входа -->
        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="34"/>
            <Setter Property="Margin" Value="0,16,0,0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderBrush" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="{StaticResource PrimaryDarkColor}"/>
                </Trigger>
                <Trigger Property="IsEnabled" Value="False">
                    <Setter Property="Background" Value="#AAA"/>
                    <Setter Property="Cursor" Value="Arrow"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль кнопки выхода -->
        <Style x:Key="LogoutButtonStyle" TargetType="Button">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="#FF424242"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#FF616161"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Grid x:Name="MainGrid" Background="#F5F5F5">
        <!-- Состояние 1: Форма логина -->
        <Grid x:Name="LoginFormGrid" Background="Transparent">
            <Border x:Name="RootCard"
                    Width="380"
                    Padding="24"
                    Background="{StaticResource CardBackgroundBrush}"
                    CornerRadius="12"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>

                    <!-- Верхняя панель: заголовок -->
                    <StackPanel Grid.Row="0" Margin="0,0,0,12">
                        <TextBlock Text="Furniture CRM"
                                   Style="{StaticResource TitleTextStyle}"/>
                        <TextBlock Text="Вход в систему управления фабрикой"
                                   Foreground="#777"
                                   FontSize="12"
                                   Margin="0,4,0,0"/>
                    </StackPanel>

                    <!-- Поля ввода -->
                    <StackPanel x:Name="LoginInputsPanel" Grid.Row="1">
                        <TextBlock Text="Логин" Style="{StaticResource LabelTextStyle}"/>
                        <TextBox x:Name="LoginTextBox"
                                 Style="{StaticResource InputTextBoxStyle}"/>

                        <TextBlock Text="Пароль" Style="{StaticResource LabelTextStyle}"/>
                        <PasswordBox x:Name="PasswordBox"
                                     Style="{StaticResource InputPasswordBoxStyle}"/>

                        <Button Content="Войти"
                                x:Name="LoginButton"
                                Style="{StaticResource PrimaryButtonStyle}"
                                Click="LoginButton_Click"/>
                    </StackPanel>

                    <!-- Сообщение об ошибке (старое, не используется) -->
                    <TextBlock x:Name="ErrorMessageTextBlock"
                               Grid.Row="2"
                               Foreground="#D32F2F"
                               FontSize="12"
                               Margin="0,12,0,0"
                               TextWrapping="Wrap"
                               Visibility="Collapsed"/>
                </Grid>
            </Border>

            <!-- Всплывающая панель ошибки -->
            <views:ErrorToastUserControl x:Name="ErrorToast"
                                         HorizontalAlignment="Stretch"
                                         VerticalAlignment="Top"
                                         Margin="16,16,16,0"
                                         Panel.ZIndex="10"/>
        </Grid>

        <!-- Состояние 2: Приветствие -->
        <Grid x:Name="WelcomeGrid" 
              Background="#F5F5F5"
              Opacity="0"
              Visibility="Collapsed">
            <TextBlock x:Name="WelcomeTextBlock"
                       FontSize="36"
                       FontWeight="Bold"
                       Foreground="#1976D2"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"/>
        </Grid>

        <!-- Состояние 3: Панель управления -->
        <Grid x:Name="MainPanelGrid"
              Background="#F5F5F5"
              Opacity="0"
              Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Контент панели управления (будет заполнен динамически) -->
            <ContentControl x:Name="MainPanelContent" Grid.Row="0"/>

            <!-- Кнопка выхода -->
            <Button Content="Выйти"
                    Grid.Row="1"
                    Style="{StaticResource LogoutButtonStyle}"
                    HorizontalAlignment="Stretch"
                    Margin="18,0,18,18"
                    Click="LogoutButton_Click"/>
        </Grid>
    </Grid>
</Window>
