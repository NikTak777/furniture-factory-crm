<UserControl x:Class="FurnitureCRMClient.Views.DirectorStaffSalesAnalysisTab"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    mc:Ignorable="d"
    d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style x:Key="HeaderTextStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
        </Style>
        <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
        </Style>
    </UserControl.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Анализ продаж персонала" Margin="0,0,0,10" Style="{StaticResource HeaderTextStyle}"/>

        <!-- Фильтры -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10" VerticalAlignment="Center">
            <TextBlock Text="Период: " VerticalAlignment="Center" Margin="0,0,5,0"/>
            <TextBlock Text="с" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="FromDatePicker" Width="130" Height="23" Margin="0,0,10,0"/>
            <TextBlock Text="по" VerticalAlignment="Center" Margin="0,0,5,0"/>
            <DatePicker x:Name="ToDatePicker" Width="130" Height="23" Margin="0,0,10,0"/>
            <Button Content="Обновить анализ" Click="RefreshAnalysis_Click" Padding="10,3" />
        </StackPanel>

        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Анализ по менеджерам -->
            <Grid Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,5">
                    <TextBlock Text="Эффективность менеджеров" Style="{StaticResource SectionHeaderStyle}"/>
                    <Button Content="Сбросить выбор менеджера"
                            Height="23"
                            Margin="15,0,0,0"
                            Padding="8,2"
                            Click="ClearManagerSelection_Click"/>
                </StackPanel>

                <DataGrid Grid.Row="1"
                          x:Name="ManagersDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding ManagerSummaries}"
                          SelectionChanged="ManagersDataGrid_SelectionChanged"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID менеджера" Binding="{Binding StaffId}" Width="Auto"/>
                        <DataGridTextColumn Header="ФИО менеджера" Binding="{Binding StaffFullName}" Width="2*"/>
                        <DataGridTextColumn Header="Всего заказов" Binding="{Binding TotalOrders}" Width="Auto"/>
                        <DataGridTextColumn Header="Выполнено" Binding="{Binding CompletedOrders}" Width="Auto"/>
                        <DataGridTextColumn Header="Отменено" Binding="{Binding CancelledOrders}" Width="Auto"/>
                        <DataGridTextColumn Header="Выручка (выполненные)" Binding="{Binding TotalRevenue}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- Анализ по номенклатуре -->
            <Grid Grid.Row="1" Margin="0,10,0,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Text="Популярность и выручка по номенклатуре"
                           Style="{StaticResource SectionHeaderStyle}"/>

                <DataGrid Grid.Row="1"
                          x:Name="ProductsDataGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          ItemsSource="{Binding ProductSummaries}"
                          ScrollViewer.VerticalScrollBarVisibility="Auto"
                          ScrollViewer.HorizontalScrollBarVisibility="Auto">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Артикул товара" Binding="{Binding ProductId}" Width="Auto"/>
                        <DataGridTextColumn Header="Наименование товара" Binding="{Binding ProductName}" Width="2*"/>
                        <DataGridTextColumn Header="Всего заказов" Binding="{Binding TotalOrders}" Width="Auto"/>
                        <DataGridTextColumn Header="Общее количество" Binding="{Binding TotalQuantity}" Width="Auto"/>
                        <DataGridTextColumn Header="Выполнено заказов" Binding="{Binding CompletedOrders}" Width="Auto"/>
                        <DataGridTextColumn Header="Выручка (выполненные)" Binding="{Binding TotalRevenue}" Width="Auto"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
        </Grid>
    </Grid>
</UserControl>

